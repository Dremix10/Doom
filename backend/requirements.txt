# Direct dependencies. Pinned-enough for a hackathon; see requirements.lock.txt
# for the exact resolved versions this was built and tested against.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.6
SQLAlchemy>=2.0
python-dotenv>=1.0
numpy>=1.26
python-multipart>=0.0.9
google-genai>=0.3
pywebpush>=2.0
twilio>=9.0
# Postgres / Tiger Data (Timescale). Not needed for local SQLite.
psycopg[binary]>=3.1
